export type JSONAlignement = {
  count: number;
  results: Array<{
    index: string;
    name: string;
    url: string;
  }>;
};

export type JSONEspece = {
  count: number;
  results: Array<{
    index: string;
    name: string;
    url: string;
  }>;
};
